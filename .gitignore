# Cloud Functions dependencies — installed via `npm install` inside functions/
functions/node_modules/

# Firebase CLI local state and logs
.firebase/
firebase-debug.log
firebase-debug.*.log

# Service account keys — NEVER commit these. Used only locally, once, to run
# functions/scripts/bootstrap-first-admin.js. See functions/README.md.
serviceAccountKey.json
functions/scripts/serviceAccountKey.json
